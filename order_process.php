<?php
// Отримання даних з форми
$name = $_POST['name'];
$email = $_POST['email'];
$product = $_POST['product'];
$quantity = $_POST['quantity'];

// Валідація даних (приклад)
if (empty($name) || empty($email) || empty($product) || empty($quantity)) {
    echo "Будь ласка, заповніть всі поля.";
    exit;
}

// Збереження даних в базу даних (приклад)
// ... (тут буде ваш код для підключення до бази даних і запису даних)

// Відправка email (приклад)
// ... (тут буде ваш код для відправки email)

// Відображення підтвердження
echo "<h2>Дякуємо за ваше замовлення!</h2>";
echo "<p>Ми скоро з вами зв'яжемося.</p>";
?>